# Frontend environment variables
# If set, the hero form will call the Node mock Express backend instead of FastAPI for a simple response.
# Example: http://127.0.0.1:5055
NEXT_PUBLIC_ALSS_NODE_API_URL=

# Existing API (FastAPI) base, defaults to 127.0.0.1:8789 if unset
# NEXT_PUBLIC_ALSS_API_URL=
