"use client"

import UrlForm from "@/components/scan/url-form"

export default function ScanForm() {
  return (
    <UrlForm variant="hero" hideLabel className="rounded-xl border border-white/20 bg-white/5 backdrop-blur-lg" />
  )
}
